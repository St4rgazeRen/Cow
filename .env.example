# ============================================================
# 比特幣投資戰情室 (Cow) — 環境變數設定模板
# ============================================================
# 使用說明:
#   1. 複製此檔案為 .env：  cp .env.example .env
#   2. 填入你的真實 API Key
#   3. .env 已在 .gitignore 中排除，不會被 commit 到 Git
# ============================================================

# --- Binance API (用於即時價格、資金費率、歷史數據) ---
# 若未設定，程式會使用 Binance 公開 API（不需 Key 的部分仍可運作）
# 取得方式：https://www.binance.com/en/my/settings/api-management
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

# --- LINE Bot (Task #9: 推播通知) ---
# 取得方式：https://developers.line.biz/console/
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token_here
# 你自己的 LINE User ID（用於點對點推播）
# 取得方式：使用 LINE Messaging API 的 getProfile 端點
LINE_USER_ID=Uxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- 可選設定 ---
# 無風險利率 fallback（若 DeFiLlama API 不可用時使用，小數格式）
# 例如: 0.05 = 5%
RISK_FREE_RATE_FALLBACK=0.04
