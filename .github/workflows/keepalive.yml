name: Streamlit Keepalive (防休眠 Ping)

# ==============================================================================
# 目的: Streamlit Community Cloud 免費版 7 天無人訪問後會進入休眠
#       此 Workflow 每 24 小時自動 Ping 一次 App，防止休眠
# 設定: 請在 GitHub Repo Settings → Secrets → Actions 加入:
#       STREAMLIT_APP_URL = https://mfyyo9qf5mymsrouxkfdgj.streamlit.app/
# 注意: curl 必須加 -L 追蹤 HTTP 302/303 重定向，否則 Streamlit 不會記錄為「訪問」
# ==============================================================================

on:
  schedule:
    # 每天 UTC 00:00 / 12:00 各執行一次 (台灣時間 08:00 / 20:00)
    - cron: '0 0 * * *'
    - cron: '0 12 * * *'
  workflow_dispatch:  # 允許手動觸發測試

jobs:
  ping:
    name: Ping Streamlit App
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 檢查 STREAMLIT_APP_URL 是否已設定
        run: |
          if [ -z "${{ secrets.STREAMLIT_APP_URL }}" ]; then
            echo "⚠️  STREAMLIT_APP_URL 未設定，請至 Repo Settings → Secrets → Actions 新增"
            exit 1
          fi
          echo "✅ STREAMLIT_APP_URL 已設定"

      - name: Ping App (追蹤重定向 + 等待喚醒)
        id: ping_main
        run: |
          echo "🌐 正在連接 Streamlit App..."

          # -L: 追蹤 302/303 重定向（關鍵！不加此旗標 Streamlit 不算作訪問）
          # --max-time 90: 允許 Streamlit 最多 90 秒喚醒
          # -w "%{http_code}": 只輸出最終 HTTP 狀態碼
          HTTP_CODE=$(curl -s -L -o /dev/null -w "%{http_code}" \
            --max-time 90 \
            --connect-timeout 15 \
            --retry 2 \
            --retry-delay 15 \
            --retry-all-errors \
            -H "User-Agent: Mozilla/5.0 (compatible; StreamlitKeepAlive/1.0)" \
            "${{ secrets.STREAMLIT_APP_URL }}")

          echo "最終 HTTP 狀態碼: $HTTP_CODE"
          echo "http_code=$HTTP_CODE" >> $GITHUB_OUTPUT

          if [[ "$HTTP_CODE" -ge 200 && "$HTTP_CODE" -lt 400 ]]; then
            echo "✅ App 回應正常 (HTTP $HTTP_CODE) — 防休眠成功"
          elif [[ "$HTTP_CODE" -eq 503 || "$HTTP_CODE" -eq 502 ]]; then
            echo "😴 App 正在喚醒 (HTTP $HTTP_CODE)，等待 60 秒後重試..."
            sleep 60
            HTTP_CODE2=$(curl -s -L -o /dev/null -w "%{http_code}" \
              --max-time 120 \
              -H "User-Agent: Mozilla/5.0 (compatible; StreamlitKeepAlive/1.0)" \
              "${{ secrets.STREAMLIT_APP_URL }}")
            echo "重試後 HTTP 狀態碼: $HTTP_CODE2"
            echo "http_code=$HTTP_CODE2" >> $GITHUB_OUTPUT
            if [[ "$HTTP_CODE2" -ge 200 && "$HTTP_CODE2" -lt 400 ]]; then
              echo "✅ App 喚醒成功 (HTTP $HTTP_CODE2)"
            else
              echo "❌ App 喚醒失敗 (HTTP $HTTP_CODE2)" && exit 1
            fi
          else
            echo "❌ App 回應異常 (HTTP $HTTP_CODE)" && exit 1
          fi

      - name: 記錄 Ping 結果摘要
        if: always()
        run: |
          echo "### 🤖 Streamlit Keepalive 報告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 項目 | 結果 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 執行時間 | $(date -u '+%Y-%m-%d %H:%M UTC') |" >> $GITHUB_STEP_SUMMARY
          echo "| 最終 HTTP 狀態 | ${{ steps.ping_main.outputs.http_code }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 重定向追蹤 | ✅ 已啟用 (-L flag) |" >> $GITHUB_STEP_SUMMARY
